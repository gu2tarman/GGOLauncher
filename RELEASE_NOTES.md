# GGO Launcher Release Notes

## v0.1.6

### 변경 사항
- 공지를 런처 실행 중에도 5분마다 백그라운드로 갱신하도록 개선했습니다.
- 공지 갱신 중에는 기존 공지를 유지해 화면이 깜빡이지 않도록 했습니다.
- 각 공지판의 최신 글 1건만 자동으로 `새소식`으로 표시하고, 뒤로 밀린 글은 `공지`로 표시하도록 정리했습니다.
- 런처 자동 업데이트 안내에 이번 버전의 주요 변경 요약을 함께 표시합니다.

### 수정 사항
- 프로필이 전혀 없는 신규 사용자가 `GGO CE 설치`를 진행할 때, 설치 위치 확인 후 아무 반응 없이 중단될 수 있던 문제를 수정했습니다.
- 신규 설치 시 저장할 프로필이 없으면 기본 프로필을 자동 생성하고 설치 경로를 연결하도록 보정했습니다.

### 검증
- `npm.cmd run build` 통과
- `cargo test` 통과
- `git diff --check` 통과

## v0.1.5

### 주요 변경 사항
- 메인 화면에 표시할 프로필을 최대 2개까지 직접 선택할 수 있도록 개선했습니다.
- 프로필 관리에서 프로필 카드를 클릭하면 `ACTIVE 1` / `ACTIVE 2`로 지정되며, 다시 클릭하면 선택을 해제할 수 있습니다.
- 선택한 프로필은 메인 화면에 바로 표시되어 사냥용, 매크로용 등 자주 쓰는 설정을 더 빠르게 전환할 수 있습니다.
- 계정별 캐릭터명을 저장할 수 있도록 추가했습니다. 캐릭터명을 입력하면 `MULTI LOGIN` 시 해당 캐릭터까지 자동 로그인됩니다.
- ClassicUO 폴더의 `Data/Profiles` 기록을 읽어 계정명과 캐릭터명 입력 후보를 표시합니다. 후보가 없어도 기존처럼 직접 입력할 수 있습니다.

### 안내
- 캐릭터명 지정 로그인 사용 중 ClassicUO의 크래시 리포트 창이 표시될 수 있습니다.
- 이 경우 런처 오류가 아니라 ClassicUO 쪽에서 표시되는 창이며, **크래시 리포트 창에서 `취소`를 누르면 정상적으로 이용할 수 있습니다.**
- 캐릭터명을 비워두면 기존처럼 서버 선택 단계까지만 자동 로그인됩니다.

### 공지 및 UX 개선
- 공지 데이터를 한 번만 받아와 Margo 공지와 GGOUO 공지 카드에 공유하도록 정리했습니다.
- 공지는 기본 접힘 상태로 표시되며, 제목을 클릭하면 내용을 펼쳐볼 수 있습니다.
- 공지 제목, 날짜, 펼침 버튼 표시를 다듬고 긴 제목도 더 자연스럽게 확인할 수 있도록 개선했습니다.
- 공지 카테고리 `이벤트`를 `새소식`으로 변경했습니다.
- 공지 본문에 바로 이동 가능한 링크 표시를 지원합니다.
- 등록된 플러그인 행의 여백에 마우스를 올렸을 때 입력 커서가 뜨던 문제를 수정했습니다.

### 수정 사항
- 메인 표시 프로필을 모두 해제했을 때 숨겨진 프로필이 실행 대상으로 남는 문제를 수정했습니다.
- 프로필은 있지만 메인 표시 프로필이 없는 경우, 첫 프로필 만들기 안내 대신 메인 표시 프로필 선택 안내를 표시합니다.
- 기존 사용자는 자동으로 상위 2개 프로필이 메인에 표시됩니다.

### 검증
- `npm.cmd run build` 통과
- `cargo test` 통과
- `git diff --check` 통과

## v0.1.4

### 변경 사항
- 마고 서버 상태 배지를 사이드바에 추가했습니다.
- 서버 상태 엔드포인트를 `ggoce-deploy/server-status.json`에서 받아오도록 준비했습니다.
- 서버 상태 체크는 TCP 연결 성공 여부와 응답 시간을 표시합니다.
- 사이드바 마지막 메뉴와 공지 카드 하단이 정렬되도록 레이아웃을 정리했습니다.
- 하단 footer에 팬메이드 고지와 런처 버전을 함께 표시하도록 정리했습니다.
- 릴리즈 노트를 태그별로 관리하고 GitHub Release 본문에 반영하도록 워크플로우를 개선했습니다.

### 수정 사항
- 서버 상태가 오프라인일 때 hover title에서 실패 사유를 확인할 수 있도록 했습니다.
- DNS 결과가 여러 주소일 때 첫 번째 주소만 보고 실패하지 않도록, 전체 timeout 안에서 순차 접속을 시도합니다.

### 검증
- `npm.cmd run build` 통과
- `cargo test` 통과

## v0.1.3

### 변경 사항
- 프로필 계정 목록에서 역할 없는 라디오 버튼을 제거했습니다.
- 일반 `PLAY`는 수동 로그인 흐름으로 유지하고, `MULTI LOGIN` 대상 선택은 체크박스만 사용하도록 정리했습니다.
- 프로필 요약 pill을 활성 계정명 대신 `MULTI n/전체계정` 형태로 변경했습니다.
- 우측 하단에 `Launcher vX.Y.Z` 버전을 작게 표시하도록 추가했습니다.

### 검증
- `npm.cmd run build` 통과
- `cargo test` 통과

## v0.1.2

### 변경 사항
- 초기 사용자 안내 배너와 첫 실행 완료 상태를 추가했습니다.
- 프로필, 플러그인, 실행 흐름을 더 쉽게 확인할 수 있도록 UX를 보강했습니다.

### 수정 사항
- 업데이트 에러 후 재시도 동작을 보강했습니다.
- `MULTI LOGIN` 선택 개수가 백엔드 최대 실행 수와 맞도록 정리했습니다.
- 플러그인 편집 중 `Escape` 취소가 blur 저장으로 다시 커밋되지 않도록 수정했습니다.

## v0.1.1

### 변경 사항
- 런처 자동 업데이트 흐름을 추가했습니다.
- 공지와 사이드바 링크를 원격 JSON에서 받아오도록 구성했습니다.
- GGO CE 설치/업데이트 흐름을 개선했습니다.

### 수정 사항
- 원본 ClassicUO 폴더 감지 시 확인 절차를 거치도록 보호 로직을 추가했습니다.

## v0.1.0

### 변경 사항
- GGO Custom 런처 초기 릴리즈입니다.
- ClassicUO 실행, 플러그인 선택, 프로필 관리, GGO CE 업데이트 기능을 제공합니다.
